<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DriveNext" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_violet</item>
        <item name="colorPrimaryDark">@color/primary_light_violet</item>
        <item name="colorAccent">@color/primary_grey</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="Theme.DriveNext" parent="Base.Theme.DriveNext" />
</resources>