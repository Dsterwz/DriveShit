<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DriveNext" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/primary_violet</item>
        <item name="colorPrimaryDark">@color/primary_light_grey</item>
        <item name="colorAccent">@color/primary_violet</item>
        <item name="android:textColorPrimary">@android:color/white</item> <!-- Цвет текста по умолчанию -->
        <item name="android:windowBackground">@color/secondary_black</item> <!-- Фон окна по умолчанию -->
    </style>
</resources>